# 받는 사람에게 보낼 것

아래 **회색 상자 하나를 통째로 복사**해서 Claude Code 앱 채팅창에 붙여 넣으면 끝난다.
설치·검증·실행까지 알아서 굴러가고, 막히면 무엇을 해야 하는지 사람 말로 알려 준다.

---

## 보내기 전에 (보내는 사람이 한 번만)

저장소가 비공개다. 받는 사람의 GitHub 계정을 먼저 초대한다.

```bash
gh repo add-collaborator HwigwanJi/kg-slide-editor <상대_깃허브_아이디>
```

이걸 안 하면 상대는 "저장소를 찾을 수 없다"만 보게 된다. **프롬프트로는 해결되지 않는다.**

---

## 붙여 넣을 프롬프트

```
케인즈그룹 장표 편집기를 이 컴퓨터에 설치하고 실행해 줘. 나는 개발자가 아니니
명령어를 나한테 시키지 말고 네가 직접 실행하고, 막히면 내가 무엇을 눌러야 하는지만 알려 줘.

순서:

1. 준비물부터 확인해. node --version, git --version, gh --version 을 각각 실행해서
   없는 것이 있으면 거기서 멈추고, 무엇을 어디서 받아야 하는지 링크와 함께 알려 줘.
   (Node.js 는 https://nodejs.org 에서 LTS, GitHub CLI 는 https://cli.github.com)

2. gh auth status 로 로그인 상태를 확인해. 로그인이 안 되어 있으면 멈추고
   "터미널에서 gh auth login 을 직접 실행하세요" 라고 알려 줘 — 이건 대화형이라 네가 못 한다.

3. 설치할 폴더를 정해. 내 문서 폴더 아래가 좋겠다. 이미 kg-slide-editor 폴더가 있으면
   덮지 말고 나한테 물어봐.

4. 저장소를 받아.
   gh repo clone HwigwanJi/kg-slide-editor
   "Could not resolve to a Repository" 가 나오면 접근 권한이 없다는 뜻이니,
   저장소 주인에게 초대를 요청하라고 알려 주고 멈춰.

5. 그 폴더에서 설치를 돌려. 몇 분 걸리고 중간에 큰 파일을 받는다.
   node tools/setup.mjs --all

   이것 하나가 의존성·미리보기 브라우저·스킬·훅·편집기 자산·도구 번들·시작 프로젝트를
   전부 맞춘다. 이미 있는 것은 건너뛰므로 몇 번을 돌려도 안전하다.
   "public/kg 를 비울 수 없습니다" 가 나오면 개발 서버가 떠 있다는 뜻이니 그걸 끄고 다시 돌려.

6. 제대로 깔렸는지 확인해. 둘 다 돌려서 결과를 나한테 보여 줘.
   npm run build                            → 통과해야 한다
   npm run lint:slides -- public/fixtures   → 지적이 몇 건 나오면 정상이다(종료 코드 1)

7. 편집기를 띄워. npm run dev 를 실행하고 http://localhost:5180 을 열어 줘.
   열면 시작 프로젝트(JDC 정성제안서 53장)가 이미 들어 있어야 한다.
   장표가 보이는지 확인하고, 안 보이면 원인을 찾아 고쳐 줘.

8. 마지막으로 나한테 이걸 알려 줘.
   - 설치된 폴더 경로
   - 편집기 주소
   - 내 장표를 새로 만들려면 무엇부터 하면 되는지 두세 줄로

작업 규칙은 저장소의 AGENTS.md, 장표 워크플로우는 skills/keynes-group-design/WORKFLOW.md 에 있다.
설치가 끝나면 그 둘을 읽고 시작해.
```

---

## 프롬프트가 못 하는 것 세 가지

자동화의 한계를 미리 알아 두면 막혔을 때 헤매지 않는다.

| 무엇 | 왜 | 사람이 할 일 |
|---|---|---|
| Node.js 설치 | 설치 관리자가 창을 띄운다 | https://nodejs.org 에서 LTS 받아 설치 |
| `gh auth login` | 브라우저 인증이 대화형이다 | 터미널에서 한 번 실행 |
| 저장소 접근 권한 | 비공개 저장소다 | **보내는 사람**이 초대 |

이 셋만 넘기면 나머지는 프롬프트 하나로 끝난다.

## 두 번째 컴퓨터부터

같은 사람이 다른 컴퓨터에 깔 때도 위 프롬프트 그대로 쓴다.
스킬·CSS·규칙이 저장소 한 곳에서 나오므로 모든 컴퓨터가 같은 장표를 그린다.
