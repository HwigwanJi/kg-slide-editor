# 인터뷰 기록 — 이 도구는 무엇이고 왜 있는가

## 이 파일은 무엇인가

만든 사람과 나눈 대화를 그대로 쌓아 둔다. 나중에 **용어집 · 기능 지도 · 도메인 모델**을
쓸 때 이것을 근거로 삼는다.

왜 대화를 남기는가 — 코드를 읽으면 **무엇을 하는지**는 알 수 있지만 **왜 그렇게 했는지**는
알 수 없다. 실제로 그 대가를 치렀다.

- `.kg-slot` 을 "편집기 흔적" 으로 보고 내보내기에서 지웠다. 목적(떼어낸 자리를 붙들어
  형제가 안 밀리게)은 만드는 쪽 주석에만 있었고 지우는 쪽에는 없었다.
- 오버뷰 장표의 **날개** 를 세 번 다시 만들었다. "1/3 레이아웃" 이라는 형태만 알고
  목적(활성 과업은 펼침·나머지는 접힘)을 몰라, 같은 설명을 세 번 듣고서야 맞췄다.

## 규칙

- 답을 요약하지 않는다. 말한 그대로 남긴다 — 요약하면 뉘앙스가 먼저 사라진다.
- 답에서 새로 생긴 질문을 그 자리에 이어 붙인다. 꼬리를 문다.
- 내가 코드를 읽어 알 수 있는 것은 묻지 않는다. **그 사람만 아는 것**만 묻는다.
- 확정되지 않은 것은 `(미정)` 으로 남긴다. 지어내지 않는다.

---

## 1회차 — 존재 이유

### Q1. 이 도구가 없던 시절, 이 53장을 어떻게 만들었나

> 파워포인트로 직접 그렸지.
>
> **애로사항** — 대표님이 AI가 그렇게 좋다면서 AI로 그린 밤티장표 주면 그걸 일일히
> 누끼를 따서 다시그리고... 이게 뭘하는 짓인가 싶었어. 그리고 우린 인턴이 많아서
> 품질관리 이슈도 있고.

### Q2. 왜 PPT 가 아니라 HTML·CSS 인가

> **현황 인식** — 지금 AI는 뭐 발표자료용 장표는 몰라도 컨설팅 장표의 어떤 도식화
> 시각화 수준에는 못미치잖아. PPT의 도메인적인 용도가 발표자료이기도 하니까 자꾸
> AI한테 PPT 잡을 시키면 극단적인 원메세지 원페이지를 그려온다거나, 아니면 무슨
> 밤티밤티 왕밤티 나열식 정보량중심의 장표를 그리거나.
>
> 그래서 지금 어느정도 UIUX 개발 수준이 클로드쪽에서 올라온 것 같아서 이 디자인
> 시스템을 활용한 표준화된 장표(프로젝트용은 아직 못 미치지만, 사람이 노가다하는 부분,
> 그리고 인턴보다 나은 수준의 장표는 그릴수있다)정도는 그릴수있겠다는 직관에서 출발했어.

### Q3. 북극성은 셋 중 무엇인가

> **문제인식(페인포인트)** — 근데 AI 협업도구는 대부분 파이썬 빌드 기반이기 때문에
> AI가 초안을 그린 것 위에 사람이 휴머나이징이나 수정을 그려도 AI는 그걸 잘 못읽고,
> 다음 작업을 시키면 사람의 어떤 수정이나 이런걸 덮어버리는 경우가 많아.
> **그래서 이 부분이 가장 큰 병목이야.**
>
> **철학** — 내가 이 앱에서 구현하고자하는건 AI 에이전트와 시니어 컨설턴트의 심리스한
> 협업이야. AI 에이전트가 마치 원드라이브에서 PPT 작업하는 사람 중의 하나인 것 마냥,
> 어떤 중심 결과물을 두고 계속 사람이 수정했다가 AI가 이어받았다가 이런 심리스
> 플랫폼을 만드는게 목표야.
>
> 근데 너가 해준 부분은 일부 그런 경향이 있지만, **근본적인 어떤 기술적인 해결책은
> 아니다**라는 생각이 들어. **AI와 사람이 번갈아가면서 50000번 수정을 덧대어도
> 무너지지 않는 협업의 강건성**이 내가 이루고자 하는 바야.

### 여기서 확정된 것

- **북극성은 셋 중 어느 것도 아니다.** 디자인 일관성·품질 검사는 수단이고,
  AI 협업조차도 결과다. 진짜 목표는 **번갈아 쌓아도 무너지지 않는 강건성**이다.
- 경쟁 상대는 다른 장표 도구가 아니라 **"AI가 그려 준 것을 사람이 누끼 따서 다시 그리는" 그 시간**이다.
- 지금 목표 수준은 "프로젝트용 완성도" 가 아니라 **인턴보다 나은 초안 + 사람 노가다 구간 대체**다.

### 내가 보는 지금 구조의 한계 (검증 필요)

만든 사람의 "근본 해결책이 아니다" 는 판단에 동의한다. 이유는 셋으로 본다.

1. **NodeId 가 자리(position)다.** `n.2.1.0` 은 구조 경로라 원본이 바뀌면 가리키는 것이
   달라진다. `carryOverlay` 가 건져 보지만 못 옮긴 것은 손실이고, 횟수가 쌓이면 누적된다.
2. **양쪽이 쓰는 표현이 다르다.** AI 는 `source` 를 다시 그리고 사람은 오버레이를 쌓는다.
   힘이 비대칭이라 사람은 구조를 못 바꾸고, 사람의 수정은 영원히 "덧씌움" 이다.
   원드라이브 비유가 성립하려면 둘이 **같은 자격**이어야 한다.
3. **의도가 남지 않는다.** "이 박스를 넓혔다" 가 숫자로만 저장되므로, 원본이 바뀌면
   그 숫자가 무엇을 하려던 것이었는지 알 수 없다.

---

## 2회차 — 강건성이란 무엇인가

### Q4. "무너진다" 를 실제로 본 장면은 무엇인가

> 1,2,3 다 나쁘지. 사라짐이 제일 치명적이지. 엉뚱한 데 붙음은 본적은 없어. 시각적 붕괴?
>
> 내가 말하는 무너진다라는건 그런거야. **내가 만약에 _ver0.1을 작업했어. 그럼 AI는
> ver0.1을 이어받아 작업해야하는데 ver0.0위에서 작업한다거나 그런거지.** 아니면 이제
> 너가말한 비대칭 수정을 한다거나.
>
> AI가 지금 프로젝트 밖에 있는 구조이기 때문에 정확한 네이티브는 어렵겠지만 어쨌든
> **스킬과 훅구조를 통해서 가급적 이 프로젝트 안에 들어와서 작업하는 것 처럼**이
> 내가 생각하는바야.

### Q5. AI 가 구조를 바꿔야 할 때 어떻게 되어야 하나

> **AI는 사람이 할 수 있는 모든 편집을 할 수 있어야해. 구조수정, 내용수정 등등
> AI와 사람의 권한과 기능은 모두 100대100이야.** 다만 AI가 특화된, 전체 프로젝트의
> 일관성을 점검한다던가 (ex. 소제목 폰트를 16px로 통일).

### Q6. 원드라이브 비유 — 동시인가 번갈아인가

> **동시일 필요는 없어. 다만 sync는 되어야해.** 사람이 저장한거 → 사람이 작업중단 →
> AI가 작업 → AI가 작업중단 → 사람이 이어서 작업. **이걸 50000회해도 이 워크플로우에
> 결함이 없어야한다는거야.**

### Q7. 충돌하면 누가 이기나

> Q7은 이제 일어날 일 없는 일이지? 가장 마지막으로 작업한 사람이 이기는거지.

### 여기서 확정된 것 — 문제가 완전히 달라졌다

- **동시 편집이 아니다. 턴제다.** CRDT·OT 는 필요 없다. 훨씬 다룰 만한 문제다.
- **충돌 해소도 필요 없다.** 턴제라 같은 시각에 두 손이 닿지 않는다.
- 그러므로 강건성 = **핸드오프 무결성**. 세 가지로 좁혀진다.
  1. 넘겨받는 쪽이 **반드시 최신 상태**를 본다 (ver0.0 에서 시작하지 않는다)
  2. 넘겨받는 쪽이 상대가 한 일을 **읽을 수 있다**
  3. 아무도 남의 것을 **덮지 않는다**
- **권한 100:100.** AI 도 구조를 바꿀 수 있어야 한다. 지금 비대칭의 근원은
  AI 만 `source` 를 다시 그릴 수 있다는 점이다 — 그런데 그것이 바로 오버레이를 깨는 행위다.
  뒤집어 말하면 **AI 가 `source` 재작도를 버리고 커맨드만 쓰면 100:100 이 되고 비대칭도 사라진다.**
  작도(처음 그리기)와 편집(그 뒤 전부)을 가르는 선이 여기다.

### 실측 — 이 실패가 이번 세션에 실제로 일어났다

`carryOverlay` 로 두 덱의 고아 패치(가리키는 노드가 원본에 없는 편집분)를 셌다.

```
A (편집기가 쓰던 것)   성한 편집분 747 · 허공을 가리키는 것  0
B (AI 가 재작도한 것)  성한 편집분 722 · 허공을 가리키는 것  4   ← 17p 하나에 몰림
```

17p 는 AI 가 `tools/` 에 임시 스크립트를 만들어 **`source.html` 을 직접 갈아끼운** 장표다.
`<aside class="side">` 를 `<aside class="wing">` 으로 바꾸면서 그 아래 노드 경로가 밀렸고
사람의 편집분 4건이 허공을 가리키게 됐다. **아무도 알려주지 않았다.**

D2 에 "AI 는 원본을 다시 그리지 않고 커맨드만 발행한다" 고 적혀 있다.
**규칙은 있었고 지켜지지 않았다.** 규칙을 강제하는 장치가 없기 때문이다.

같은 세션에 두 폴더 사고도 났다 — AI 가 B 에서 작업하는 동안 사람은 A 에 있었다.
53/53 이 갈렸다. 이것이 "AI 가 ver0.0 위에서 작업한다" 의 교과서적 사례다.

---

## 3회차 — 핸드오프는 실제로 어떻게 일어나는가

### Q8. 지금 파일·버전을 실제로 어떻게 관리하나

> 버전을 여러가지 만들라는 게 아니고 **어떤 지점이나 시점**이라는 설명을 하고싶어서
> 한 얘기야.

→ 파일명 버전 관리는 아니다. `_v0.1` 은 "그 시점의 상태" 를 가리키는 말이었을 뿐이다.

### Q11. 전체 훑기 중에 사람은 손을 떼나 — 여기서 설계 제안이 나왔다

> 나는 **동시작업을 안하기 위해** 요런 시스템을 했으면 좋겠어.
>
> 프로젝트 안에서 모든걸 사람이 중단하고 AI가 편집하고 이런건 너무 비효율적이고 병목이야.
> **AI가 3슬라이드를 작업하고 있으면 사람은 2슬라이드를 보면서 세부 편집을 하고
> 그럴수 있어야하잖아.**
>
> 그래서 **슬라이드에 protect on/off** 가 있으면 좋겠어. 사람이 protect를 걸면 거기를
> 이제 AI가 들어와서 이것저것 보는거지. 근데 protect가 안걸려있으면 그 슬라이드는
> 이제 AI가 아예 진입을 하지 않는거야.
>
> 그리고 **전체 protect를 걸면 이제 AI가 전역 수정을 할 수 있는거고.**
> 마치 라이브 게임 패치하듯이.

### 여기서 확정된 것

- **턴제의 단위가 프로젝트가 아니라 장표다.** 전체를 멈추는 것은 병목이므로,
  장표 단위로 손을 가른다. 한 장에는 한 손만 — 충돌은 여전히 구조적으로 불가능하다.
- 장표는 이미 독립 파일(D1)이라 이 분할이 저장 구조와 그대로 맞는다.
- **전역 잠금 = 일관성 작업 모드.** "소제목 16px 통일" 같은 전체 훑기는 이 모드에서만.

### 이름에 대한 의문 (확인 필요)

말한 대로면 `protect ON = AI 진입 허용`, `protect OFF = AI 진입 금지` 다.
직관과 반대다 — 보통 protect 는 "건드리지 마" 로 읽힌다.

이름이 직관과 싸우면 사고가 난다. 이번 세션에 그 대가를 치렀다 —
`.kg-slot` 이 "편집기 흔적" 처럼 생겨서 내보내기에서 지웠고, 장표가 무너졌다.

기존 용어로는 **체크아웃/체크인**(SourceSafe·Perforce)이 같은 뜻이고 뒤집히지 않는다.

---

## 4회차 — 잠금의 구멍

### Q12. 이름 — 뒤집힌 것이 맞나

> AI 입장에서 체크아웃이니까 사람입장에서 해줘야지. **우리가 체크아웃할테니까
> AI가 들어와서 작업하세요** 이렇게 가야할듯?

→ 이름을 사람의 행동 기준으로 잡자는 뜻. 다만 "체크아웃" 은 원래 뜻(내가 가져간다)과
반대로 쓰이고 있어 여전히 뒤집힌다. **동사가 아니라 상태로 두는 편이 안전하다** —
누가 지금 쥐고 있는가(`owner`)는 뒤집힐 여지가 없다.

### Q13. 낡은 것이 왜 프로젝트 폴더에 있나

> **낡은것이 프로젝트 폴더에 있는 이유를 난 모르겠어.** Deck에는 항상 최신만 작업둬주고
> 좌측 슬라이드 뷰에 안뜨는것이나 예전거는 뭐 temporary로 보낸다거나, backup으로
> 있어야지. **나는 그 slide 폴더 열어보고 깜짝 놀랬다니까?**

→ **D6("무엇이 있는가는 파일이 정한다")에 대한 정면 반박.** 그 결정 때문에 목차에 없는
파일이 폴더에 남고, 편집기 목록이 54장인데 project 파일은 53장인 상태가 생겼다.
사람은 목차를 진실로 여긴다. D6 은 도구 편의를 위해 사람의 직관을 뒤집은 결정이었다.

### Q14. 전역 모드 중에 사람이 한 장을 빼 가면

> **전역모드 중은 배타적이어야해.** AI가 들어왔다라는 Que를 주면 모든 사람 잠금은
> 잠궈야해. 계속 앱이 AI 너 작업중이니 물어보고 뭔가 비정상적으로 종료되거나 하면
> 그거에 대한 어떤 논리적인 처리도 필요한 거 같고.

### Q15. 잠금은 어디에 사는가

> 잠금을 어떻게... **그냥 JSON으로 하는건 별로야?** 별도 파일로 하면 그 슬라이드의
> 위치나 그런 정보들을 읽는 논리회로가 복잡해질거 같은데.
>
> 사람이 편집기를 닫아도 AI 작업이랑 아무 상관없는거아냐? **그 슬라이드 자체에
> protect=y라는 값 정보가 들어있으니까?**

### 그리고 — 전면 재작성을 물었다

> 그래서 이만하니, **지금까지 만들었던 앱은 참고용 구조로 두고 저장계약이나, 앱을
> 전면 재작성을 하는게 좋은가** 싶기도해.
>
> 뭔가 지금 **구 시스템의 '빚' 위에 이 작업을 하려하지말고 새 술은 새 부대에 담는다**
> 라는 철학에 따라서 생각해줘. 지금 이 시스템이 빚진것을 청산하려다가 더 큰 빚이 될
> 수도 있어. **지금 내 목표에 도달하기 위한 가장 최적의 path와 풀이**를 넌 늘 고려해줘.

---

## 5회차 — 범위가 정해졌다

### Q12 결론. UI 이름

> 그럼 그 UI의 이름은 **"Me / AI"** 이렇게 모드 토글로하자.

### Q16 결론. 마이그레이션

> **기존 장표는 신경안써도돼. 새술은 새부대에. 이제부터 본격 개발이야.**

→ 위치 id → 안정 id 이관이 통째로 사라진다. 처음부터 안정 id 로 시작하면 된다.
계약을 새로 짜는 비용이 크게 줄었다.

### Q17 결론. AI 편집 범위 — 선을 긋지 않는다

> 그냥 **사람이 할수있는 모든 작업은 AI가 한다**라고 받아드려. 자꾸 짱구굴리지마.
> 이게 근데 왜 어려워? 그냥 봐바. **PPT안에서 claude가 작업을한다고 치면 사람이 해둔
> 작업 위에서 하잖아.** 그러면 표도만들고 다 하잖아. 그냥 **그 AI모델의 성능에 따라서
> 할 수 있는 건 다 하는거지.**

→ 권한 모델을 설계할 일이 아니었다. 경계는 **모델의 능력**이지 제도가 아니다.
계약이 할 일은 "무엇을 허용할까" 를 정하는 것이 아니라, **무엇을 하든 안 무너지게** 하는 것.

### Q18 결론. 못 하는 것을 만나면 — 멈추지 않고 남긴다

> 그냥 이래. **AI가 할 수 있는 수정은 다하고 코멘트를 남기도록** 해주면 될거같아.
> 아 이런부분은 좀 한계가 있었다 등.

→ 기다리지도 건너뛰지도 않는다. **하고, 못 한 것을 적는다.**
이것이 앞서 정리한 핸드오프 3원칙 중 2번(넘겨받는 쪽이 상대가 한 일을 읽을 수 있다)의
구체적 형태다. 코멘트는 곁들이는 기능이 아니라 **핸드오프의 일부**다.

### Q19 결론. 새 저장소로 간다

> A로 하면 좋겠네.

→ 지금 저장소는 참고용으로 남긴다. 디자인 시스템·검사 규칙·위계 추론은 옮겨 온다.
`carryOverlay`·`D6`·위치 id 를 전제로 쓰인 코드는 애초에 없는 상태에서 시작한다.

### Q20 결론. 인수인계는 "슬라이드 메모"다

> 인수인계노트는 **슬라이드에 대한 메모(마치 pdf와 같은 기능)에 누적식으로** 남았으면 좋겠어.

→ 별도 리포트가 아니라 **장표에 붙는 메모**이고, 덮어쓰지 않고 **쌓인다.**
PDF 주석과 같은 성격 — 사람도 쓰고 AI 도 쓴다.
계약에 필드가 하나 더 생긴다(`notes`). 이것이 아래 지적의 산 예다.

### 진행 방식에 대한 지적 — 인터뷰를 접으려 한 것을 물렸다

> 근데 **핵심 기능같은거에대해서는 하나도 안하고** 이제 App의 철학에 대해서 설명한
> 거같은데 벌써 닫으려고? 만약 **내가 원하는 기능을 하나둘말할때마다 저장계약이나
> DB스키마 이런것들이 더 많아질 수도 있잖아.**

→ 맞다. 지금까지 나온 것은 **협업 뼈대**뿐이고 제품이 무엇을 하는지는 안 물었다.
기능이 계약을 결정하지 계약이 기능을 결정하지 않는다. 기능부터 훑는다.

---

## 6회차 — 그래서 이 앱은 무엇을 하는가

### Q21. JDC 53장을 만들 때 실제 순서

> (답변 대기)

### Q22. 그중 사람이 제일 오래 붙들고 있던 단계

> (답변 대기)

### Q23. 새 앱에서 제일 먼저 자동화되기를 바라는 단계

> (답변 대기)

### Q24. 지금 앱에 없어서 아쉬웠던 기능

> (답변 대기)

> Q21~Q24 는 답을 듣지 못한 채 넘어갔다. 대신 와이어프레임 구상이 나왔다.
> 작업 흐름 전체는 아직 안 물었다 — **남은 숙제.**

---

## 7회차 — 와이어프레임과 코호트

### Q25. 와이어프레임 구상

> 일단 이번에 디자인 시스템을 하니까 드는 생각이 **"와이어프레임"** 시스템이야.
> 편집기 내에서 새로운 프로젝트를 생성하고 **사람이 와이어프레임을 템플릿으로 잡아두고
> 체크아웃한다음** 이제 AI 채팅(스킬on)에서 요 장표를 이런내용으로 채워줘라고하면,
> 이 와이어프레임 안에서 뭘 채우고 이런 것들을 할 수 있지 않을까 해서.
> **지금은 좀 너무 세로로만 길다거나 가로로만 길다거나** 이런 부분이 있어서.
>
> 와이어프레임은 이제 컴포넌트라는 요소들의 합일텐데 **컴포넌트는 좀 변경할 수 있게.**
> 의도한대로 작동할까? 지금 AI 발전수준에서?

**실험으로 답했다.** 22p 를 넣고 골격을 뽑아 보니 깨끗하게 떨어졌다
(`268px │ 3fr 2fr 2fr 2fr` · 날개 아코디언 + 카드 3열). KG 장표가 이미 컴포넌트 어휘로
지어져 있어 구조가 규칙적이기 때문이다.

세션 안에 증거도 있었다 — **날개를 세 번 실패했는데, 22p 구조를 주고 "이걸 옮겨라" 고
했을 때는 한 번에 됐다.** 같은 모델·같은 세션, 골격이 있었는지만 달랐다.

한계 셋: (1) 같은 골격이 이름만 달라 중복으로 잡힌다 (2) 골격만으로 품질은 안 정해진다
— 22p 와 17p 는 구조가 거의 같았는데 하나는 좋고 하나는 밤티였다 (3) SVG 도식 안쪽 논리는
못 담는다. 그건 도형 라이브러리 몫이다.

### 와이어프레임은 첫 작도 때만 유효하다

> 와이어프레임은 일단 **AI의 첫 작도때에만 유효**할 가능성이 높아.
> 그 뒤에는 어느정도 편집의 자율성을 주는 게 맞을듯.

→ 구속이 아니라 **출처 기록**이 된다. cascade 문제가 사라진다.

### 와이어프레임 탭

> 아예 **와이어프레임이라는 메뉴탭**이 있었으면 좋겠어. 별도 라이브러리를 띄우는 부분과
> (템플릿 개념) 그 템플릿을 띄운 뒤에 개별 와이어프레임 영역에 대해서 **타입을 바꾼다던지**
> 이런 편집부분이 있을 거 같은데.

### Q29. 테스트 TF — 무엇을 성공으로 보나

> 와이어프레임을 그리고나서 지금 스킬대로 이제 장표를 작도를 해본다음
> **마음에 드냐 안드냐**, **그안에 있는 규칙들을 준수하냐 안하냐** 이거지뭐.

→ 합격 기준이 둘이다. 하나는 사람 판단, 하나는 기계 판단(검사).
격자가 맞았나 같은 중간 지표는 두지 않는다 — 결과물로 본다.

### Q30. 영역 타입 — 내가 든 축이 틀렸다

> 와이어프래임탭에서 영역타입이라는건 너가 생각하는 개념은 아닌거 같고, 이 안에 어떤
> **코호트**(도식이냐, 텍스트박스냐, 키워드 박스냐, 플로우 차트냐 등등)에 대한 얘기야.
> **절대 임의로 만들지말고 나랑 하나하나 설계할 것.**

→ 내가 적었던 `fixed/grid/module/figure/free` 는 **앱이 그 영역을 어떻게 다루나**였다.
물은 것은 **그 자리에 들어가는 것이 무엇이냐** 다. 다른 축이다.
**코호트 목록은 임의로 만들지 않는다. 하나씩 같이 정한다.**

---

## 8회차 — 코호트 설계 (진행 중)

### 있는 것부터 세었다 (관찰이지 제안이 아니다)

디자인 시스템 카탈로그에 모듈이 **23종** 있는데 53장에서 실제로 쓰인 것은 **5종**뿐이다.

```
kgx-flow 7 · kgx-trapez 5 · kgx-tube 2 · kgx-venn 1     (kgx-tube 는 카탈로그에 없다)
```

나머지는 전부 장표가 **직접 만든 구조**다. 같은 개념이 이름만 달리해 흩어져 있다.

```
머리+본문 카드   .bx 5장 · .blk 5장 · .card 5장 · .col 10장
표               .tbl 6장 · .tr 5장 · .th 6장 · .cell 5장
키-값            .kv 4장
주석·시사점      .note 5장
과업 띠          .nav 8장
날개 항목        .task 4장
격자(그릇)       .wrap 23장 · .cols 14장 · .cols2 5장 · .c3 4장
```

이것이 **"디자인스킬의 컴포넌트를 잘 이해를 못하고 그냥 냅다 박스박스로 해버린다"** 의 실측이다.
카탈로그 23종 중 5종만 쓰고 나머지는 매번 새로 박스를 만들었다.

### 먼저 정할 것 — 든 예 넷이 서로 다른 층에 있다

```
도식          큰 갈래
플로우차트     도식의 한 종류
텍스트박스     그릇
키워드박스     그릇 + 내용 성격
```

어느 층에서 코호트를 자를지부터 정해야 그 아래가 정리된다.

### 두 층으로 자르기로 했다

> 음 좋아 **두층으로 짜른다는 거 좋네.**

### 인프라 먼저인가 몇 개 보는 게 먼저인가

> 근데 일단 인프라가 없어서 인프라를 먼저만드는게 좋아 아니면 일단 몇개 보고
> 방향을 잡고가는게 좋아?

→ 보는 것이 먼저다. 목록 크기를 모르면 자료구조도 UI 도 틀린다. 경계 사례가 설계를 정한다.
멈출 기준은 **새 코호트가 두 장 연속 안 나오면 멈춘다.**

### 실제 장표로 시작 — 첫 단서는 "소주제"

> 여기서 내가 주목하는 건 **"소주제(위계)"**에도 있는데 각기 다른 예시야.
> 첫번째 두번째 세번째 장표가 **소주제를 어떻게 나누는지** 한번 구성을 유심하게 봐바.
> 이게 **장표의 가장 구조적인 부분을 결정하는 첫번째 단서** 중에 하나이거든?

(장표 셋 — 인사평가 진단 계열: ① 설문유형/이슈분석/개선방향 ② 부서별 점수 분포 비교
③ 평가별 분석 종합)

### ★ 핵심 원칙 — 띠 소주제와 블록 소주제

> 그렇지 꽤 괜찮아. **띠 소주제와 블록 소주제가 있어.**
>
> **띠 소주제로 그냥 나눠서 표현하는 경우**도 있고
> **띠 소주제를 크게 그리고 밑에 블록 소주제로 표현하는 경우**도 있어.
>
> 세번째는 좀 잘못 분석하긴 했는데 저거는 이제 **블록소주제만 있는 경우**야(띠 소주제는 생략).
>
> **이게 내가 발견한 장표의 핵심 원칙중 하나야. 이것을 기억하도록!**

### 그리고 곧바로 다시 잡았다 — 소주제는 "가장 넓게 뻗은 것"

> 응 아니야 다시 바로잡을게. **팁업적평가 개인업적평가 개인역량평가는 소주제는 아니고
> 그 하위단**이야. **가장 긴거 하나를 그냥 소주제 단위로 봐줘.** 물론 소주제는 3열
> 블록일수도 있고 다양하기도 한데 어쨌든.
>
> 그 밑에 이제 내용들이 채워질거아냐. 뭐라고하지.. **컨테이너?**
> 용어집을해야하니까 그것도 생각하자.

```
띠 소주제     가로로 뻗어 그 아래를 통째로 덮는다        한 장에 하나      장표2 · 장표3
블록 소주제   나란히 놓여 서로 다른 영역을 이름 짓는다    한 장에 여럿      장표1
```

| 장표 | 소주제 | 그 아래 |
|---|---|---|
| 1 | 설문유형 · 이슈 분석 · 개선방향 (블록 3) | 좌: 1~4 목록 · 중: 1~3 이름 붙은 묶음 · 우: 이름 없는 박스 3 |
| 2 | 부서별 점수 분포 비교 (띠 1) | 이름 붙은 묶음 2 (팀 업적 포함 여부 / 부서별 개인업적·역량) |
| 3 | 평가별 분석 종합 (띠 1) | 이름 붙은 묶음 3 (①팀업적 ②개인업적 ③개인역량) |

**아래 층의 이름은 아직 미정.** "컨테이너" 가 후보로 나왔다.

→ 이름은 **컨테이너**로 정해졌다. 자세한 것은 용어집.

---

## 9회차 — PPT + C4D 하이브리드

> 내가 하고싶은건 두가지개념의 하이브리드야. **PPT + C4D**.
> PPT는 왜 **하나하나 도형을 그려야하지?** 근데 또 **프리셋? 너무 자유도가 낮아.**
> C4D는 기본 도형에 세그먼트·높이 등등 하고, morph 쓰고 툴로 깎아서 별의별걸 다 만들지?

**깨달은 것** — 카탈로그(kgx 23종)가 실패한 까닭은 개수가 적어서가 아니라 **프리셋이라서**다.
5단계 셰브론뿐인데 4단계가 필요하면 못 쓴다 → 박스를 새로 그린다 → `.bx`·`.blk`·`.card` 중복.
**개수를 늘려도 안 풀린다. 중간값이 없기 때문이다.**

`kgx-flow`·`kgx-chev`·`kgx-steps` 는 다른 부품이 아니다 — **상자를 가로로 반복** 하는
같은 원시형에 연결·화살촉·번호라는 다른 변형이 붙은 것이다.

```
프리셋   완성품 N개 중 고른다            → N가지
C4D     원시형 × 파라미터 × 변형 조합    → 곱셈으로 폭발
```

컨테이너가 바로 그 원시형이다. "안에 컨테이너가 또 들어간다" = 계층,
"배치는 무궁무진" = 파라미터 공간. **이미 C4D 식으로 말하고 있었다.**

AI 협업에 결정적인 까닭 — 프리셋에서는 맞는 부품이 없으면 **이탈할 수밖에 없다.**
"냅다 박스박스" 의 원인이 게으름이 아니라 **선택지 부재**였다.

### 파라메트릭 원시형의 생애

> 표로 그리면 성의없다 그러거든? 그래서 **컨설턴트는 하나하나 박스로 그려.**
> 행열수를 정하면 박스가 다 생성. 그때는 **폭 균등 옵션이 켜져 있어서** 편집이 안되는 상태.
> → 간격·박스 색·테두리·그림자(거의 photoshop급) → 균등을 빼고 드래그 핸들로 조정
> → 병합 → 개별로 손대야 하면 **bake.** 그럼 원시로서의 값은 잃고 **개별 shape 속성이 남는다.**

**결과는 박스인데 생성은 표다.** 지금 시스템은 이 둘을 못 나눈다 — 표로 그리면 표가 되고
div 로 하나하나 그리면 노가다다. 파라메트릭 생성이 그 사이를 잇는다.
**자유도를 한꺼번에 주지 않고 단계적으로 푼다** — 처음부터 열면 밤티, 끝까지 잠그면 프리셋.

### 생애주기는 순서가 아니라 상태다 (지적 받음)

> 생애주기는 글쎄.. **나라면 이렇게 작업한다이지, 모든 작업자가 같은 테크트리를 탈거같진 않아.**

```
파라메트릭 상태  ──(bake · 비가역)──▶  개별 도형 상태
```

색·균등·병합의 순서는 자유. **순서가 있는 것은 bake 하나뿐이다.**

### bake 는 전체 (확정)

> 베이크는 전체 make editable이 맞지 싶은데.. 부분은 복잡할듯.

→ 뜻밖의 이득: **경계가 명확해진다.** 파라메트릭 = AI 가 통째로 다시 만들어도 되는 것,
bake 됨 = 사람이 손댄 것. 부분 bake 면 "반쯤 사람 것" 이 생겨 매번 따져야 한다.

### 머리행 옵션은 넣는다 — 초심자용이 아니라 의도를 남기려고

> 완전 자유도를 주려면 사실 필요없는 기능이긴 하잖아. primitive 속성에서 **행선택 열선택**
> 옵션을 줘서 행을 선택한다음 간격을 줄이고 색상만 부여하면 되니까.
> 근데 뭔가 조금 더 **초심자를 위한 그정도 기능**은 제공하는 게 좋을까?

결과 픽셀은 같지만 **뜻이 남지 않는다.** 나중에 "머리행 색 바꿔줘" 라고 하면 어느 행이
머리였는지 추측해야 한다. 검사도 못 건다("머리행은 h3 위계여야 한다").

특별대우는 하지 않는다 — **행·열에 역할을 붙이는 자리**를 두고 지금 값은 `머리` 하나만.
합계가 필요해지면 그때 늘린다. 배타적이지도 않다 — 켜서 기본 서식을 얹고 그 위에
행 선택으로 덮어쓸 수 있으면, 초심자도 숙련자도 같은 것을 쓴다.

### 원시형 — 내가 오독한 것을 바로잡았다

건설근로자공제회 Fulldeck 385장에 도형 수·연결선·비사각형 지오메트리로 점수를 매겨
도식이 많은 장을 뽑았다(97p 조직도 · 193p Value Chain · 252p 지사승격기준 · 278p 재설계안).
겉모습이 달라도 재료가 겹쳤다 — 97p 와 278p 는 같은 트리고, 193p 도식이 252p 에 축소돼 다시 나온다.

내가 "반복" 하나로 뭉갠 것을 바로잡았다.

> 기본도형 + **repeater** + **slicer** 이런 조합은 어때?

```
슬라이서   주어진 영역을 나눈다   영역이 먼저, 개수가 나중.  전체 크기 고정
리피터     개수만큼 만든다       개수가 먼저, 영역이 나중.  전체가 늘어남
```

**표 워크플로우는 처음부터 슬라이서였다.** 결정적 증거 — "드래그핸들로 **전체 폭 내에서** 조정".
리피터면 폭을 늘릴 때 전체가 커진다.

C4D 용어로 리피터는 **Cloner**(MoGraph), 자르기는 Knife·Voronoi Fracture·Boole,
클론을 파라메트릭 연결을 끊지 않고 바꾸는 것이 **Effector** 다.

> 단계도형에 repeater 먹이면 단계형이 되고, effector 로 맨왼쪽 움푹 팬 부분을 메꿀지.
> repeater 도 왼쪽에 **5 → 3 → 1** 이런식으로 줄어드는 구조를 깔아두고 에디터블해서 조정,
> randomize 로 가운데정렬을 살짝 틀어줄수도. **이거 바이브코딩으로 가능?**

→ 가능하다. 다만 갈린다 — **계산은 순수 함수라 빠르고, 핸들 조작은 느리다.**
이 저장소 10,353줄 중 제일 고생한 것이 `transform.pointer.ts` 606줄이었고
드래그 점프 버그는 재현조차 못 했다. **핸들 없이도 AI 협업은 완성된다**(커맨드로 파라미터를 만지므로).
C4D 도 속성 패널의 숫자가 진실이고 뷰포트 핸들은 편의다.

함정 — **randomize 는 시드를 문서에 저장해야 한다.** 안 그러면 열 때마다 그림이 달라지고
화면과 내보내기가 어긋난다. 나중에 발견하면 계약을 고쳐야 한다.

`Step` 은 순서에 따라 값이 점점 변하는 이펙터다(5→3→1). "맨 왼쪽만 다르게" 는 Step 이 아니라
**선택**이다 — 앞서 말한 `행 선택 / 열 선택` 이 바로 그것이었다.

---

## 10회차 — 컨테이너가 모든 것을 담는다

> 컨테이너가 이제 어떤 **영역(필드)** 의 개념이라고 했는데, **클릭하면 그 컨테이너에 "도식"을
> 그린다라는 사용자의 결정이 있을때** 요 휴리스틱편집을 하도록 하는거야.
>
> **Plain** 을 선택하면 그 공간은 이제 fill(pattern, color) stroke,
> **image** 를 선택하면 이미지넣기 뭐 이렇게 되는건데.
>
> **split containers, merge containers** 도 필요하고.
>
> 이미지든 텍스트든 도식이든 **컨테이너안에 포함**되어 해당 시스템은
> **벤토그리드처럼** 관리될 수 있어야해.

→ 앞의 것들이 여기서 하나로 묶인다. 용어집에 확정해 넣었다.

```
컨테이너 = 사각형 영역(필드)
  나뉜 컨테이너   안에 자식을 가진다.  split / merge
  채워진 컨테이너  내용 종류를 하나 가진다.  도식 · 평면 · 이미지 · 글
컨테이너 층은 언제나 벤토그리드 — 겹치지 않고 빈틈없이 채운다
자유 배치는 컨테이너 안에서, 도식을 bake 한 뒤에만 생긴다
```

> (진행 중 — 아래 세 가지 확인 필요)

---
