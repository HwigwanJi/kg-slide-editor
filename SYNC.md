# 다른 컴퓨터에서 최신으로 맞추기

받는 쪽 클로드 코드에 **아래 지시문을 그대로 붙여 넣는다.**
이 저장소는 코드만이 아니라 **스킬과 작업 규칙까지** 담고 있어서, `git pull` 만으로는 절반만 갱신된다.

---

## 지시문 (여기서부터 복사)

이 저장소를 최신으로 맞춰라. 코드뿐 아니라 스킬과 규칙까지 바뀌었다.

### 1. 받는다

```
git stash list          # 남겨 둔 작업이 있는지 먼저 본다
git status              # 손댄 파일이 있으면 여기서 멈추고 사람에게 알린다
git pull --rebase
npm install
npm run setup
```

`npm run setup` 을 빠뜨리면 스킬과 편집기 자산이 옛것으로 남는다.
검사·적재는 코어가 번들보다 새로우면 알아서 다시 만들지만, **스킬은 그렇지 않다.**

설치가 `✕ 설치본을 누군가 직접 고쳤습니다` 로 멈추면, 이 컴퓨터에서 `~/.claude/skills` 를
직접 고쳐 둔 것이다. 그 수정을 살리려면 저장소의 `skills/` 로 옮긴 뒤 다시 실행하고,
버려도 되면 `npm run setup -- --force`. **묻지 말고 지우지 마라.**

### 2. 통과하는지 본다

```
npm run build
npm run lint:slides -- public/fixtures
```

### 3. 바뀐 규약을 읽는다

```
AGENTS.md            §5 작업 절차 — 명령 순서가 바뀌었다
docs/WIRING.md       기능이 어디에 있고 어디로 흐르는가
docs/CONCURRENCY.md  편집기와 명령줄이 같은 파일을 쓸 때의 소유권
docs/DECISIONS.md    D6 — 무엇이 있는가는 파일이 정한다
docs/SAVE_CONTRACT.md  편집한 내용이 어디에 저장되는가
```

### 4. 이것부터 바뀐 줄 알고 시작한다

**장표를 고칠 때는 반드시 이 순서다.**

```
npm run render -- <slide.kgslide>           # 1. 지금 모습을 읽는다
npm run apply  -- <slide.kgslide> --cmds -  # 2. 커맨드를 보낸다
```

`source/*.html` 만 읽고 고치면 안 된다. 그 파일은 작도한 그대로라 사람이 편집기에서
바꿔 놓은 것이 보이지 않는다. HTML 을 다시 그리는 것도 안 된다.

**필드마다 주인이 하나다.**

| 필드 | 주인 |
|---|---|
| `source` · `canvas` | 작도 — 명령줄 |
| `patches` · `tree` · `theme` · `stack` | 사람 — 편집기 |

재적재(`npm run ingest`)는 원본만 갈아끼우고 편집분은 이어 붙인다.
옮기지 못한 것이 있으면 몇 건인지 알려 주므로, **그 수가 0 이 아니면 사람에게 그대로 전한다.**

**프로젝트 파일은 `project.kgproj`** 다. 예전 `deck.json` 도 읽는다.
무엇이 존재하는지는 `slides/*.kgslide` 파일이 정하고, 순서만 프로젝트 파일이 정한다.

**편집기 저장은 Ctrl+S 하나**다. 폴더가 없으면 그때 자리를 묻는다.
명령줄이 폴더에 쓰면 편집기 상태바에 "디스크가 더 새로움" 이 뜨고,
사람이 **다시 읽기** 를 눌러야 반영된다. 저절로 갈아 끼우지 않는다.

**헤더 메시지띠는 공백 포함 70자, 한 줄.** 검사가 잡는다.

**화면 배선이 정리됐다.** `docs/WIRING.md` 가 무엇이 어디에 있고 어디로 흐르는지 정한다.
여기 없는 길은 만들지 않는다.

| 리본 탭 | 단추 |
|---|---|
| 프로젝트 | 프로젝트 열기 · 폴더 쓰기 진단 |
| 홈 | 저장 · 다시 읽기 · 내보내기 · 되돌리기 · 복제 · 삭제 · 참조 복사 |
| 슬라이드 | 새 장표 · 장표 복제 · 장표 파일 지우기 |

- 프로젝트를 여는 길도, 저장하는 길도 **하나**다. 같은 일에 단추를 둘 두지 않는다.
- 모든 기능은 `src/app/actions.ts` 를 지난다. 화면이 `api` 를 직접 부르면
  명령 목록에도 단축키에도 안 붙고 노출 누락 점검도 못 한다.
- 되돌릴 수 없는 일은 이름을 다르게 짓는다 —
  "삭제"(Ctrl+Z 로 돌아옴)와 "장표 파일 지우기"(파일이 사라짐).
- 실패는 토스트로 알린다. 상태줄만 쓰면 아무 일도 안 일어난 것으로 읽힌다.

### 5. 확인하고 알린다

무엇이 바뀌었는지 한 문단으로 요약해 사람에게 알린다. 그 다음 무엇을 할지 묻는다.
받자마자 장표를 고치기 시작하지 마라.

## 지시문 끝

---

## 손댄 파일이 있을 때

`git pull --rebase` 가 충돌하면 **코드는 원격을 따르고, 프로젝트 폴더는 건드리지 않는다.**
프로젝트 폴더(`JDC_정성제안서` 등)는 이 저장소 밖이라 애초에 충돌하지 않는다.

스킬을 고쳤다면 저장소의 `skills/` 를 고친 것이어야 한다.
`~/.claude/skills` 를 직접 고친 것은 파생물을 고친 것이라 다음 설치에서 되돌아간다.
